<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidtraining">
<uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidTraining">
        <activity
            android:name=".MainActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".message.MessageActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".utils.ActivityUtils"
            android:exported="true">

        </activity>
        <activity android:name=".fragment.ActivityFragmentContainer"
            android:exported="true"/>
        <activity android:name=".collapseToolBar.ActivityCollapseToolBar"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".customCollapseToolBar.ActivityCustomCollapseToolBar"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:exported="true">

        </activity>
        <activity android:name=".bottomsheetdialog.MainActivityBottomSheet"
            android:exported="true">

        </activity>

    </application>

</manifest>